<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TajMahal" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TajMahal" parent="Base.Theme.TajMahal" >
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:splashScreenTheme" tools:targetApi="s">@drawable/splash_background</item>
        <item name="android:statusBarColor">@color/red</item>
    </style>


    <!-- ProgessBar -->
    <style name="CustomProgressBarStyle" parent="Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:progressTint">#FFB800</item>
        <item name="android:backgroundTint">#DADADA</item>
    </style>

    <!-- rounded ImageView -->
    <style name="roundedImageView" >
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

</resources>